{
  "name": "rust-wasm-dsl",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cross-env NODE_ENV=production rspack build",
    "check": "biome check --write",
    "dev": "cross-env NODE_ENV=development rspack serve",
    "format": "biome format --write",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@rspack/cli": "^1.2.5",
    "@rspack/core": "^1.2.5",
    "@rspack/plugin-react-refresh": "^1.0.1"
  },
  "dependencies": {
    "dsl-parser": "workspace:^"
  }
}
